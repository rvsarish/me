import React from "react"; // Importing React library

const About = () => {
  return (
    <div>About</div> // Rendering a simple div with the text "About"
  );
};

export default About; // Exporting the About component for use in other parts of the app
